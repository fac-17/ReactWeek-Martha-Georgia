import React from "react";

const Dropdown = () => {
  const [category, setCategory] = React.useState(null);
  React.useEffect(() => {});
  return (
    <div>
      <select>
        <option value="9" />
      </select>
    </div>
  );
};
